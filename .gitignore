# secrets — never commit the API key or the Firebase service account
.env
*.env.local
serviceAccountKey.json
*serviceAccount*.json
firebase-adminsdk*.json

# system junk
node_modules/
Thumbs.db
.DS_Store

.vercel

# firebase web config (public identifier, but kept out of the repo by owner preference)
firebase/firebase-config.public.js
